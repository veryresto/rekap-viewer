{
  "name": "rekap-viewer",
  "version": "1.0.10",
  "description": "A premium, mobile-friendly web application to visualize IPL (Iuran Pemeliharaan Lingkungan) payment data directly from Google Sheets. Built with pure HTML/CSS/JS for maximum performance and zero dependencies.",
  "main": "src/server.js",
  "scripts": {
    "prestart": "node scripts/generate-build-info.js",
    "start": "node src/server.js",
    "predev": "node scripts/generate-build-info.js development",
    "dev": "node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "fly deploy --build-arg VITE_GIT_SHA=$(git rev-parse HEAD) --build-arg VITE_GIT_BRANCH=$(git rev-parse --abbrev-ref HEAD) --build-arg VITE_APP_VERSION=$(git describe --tags --abbrev=0 2>/dev/null || echo '')"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1046.0",
    "@supabase/supabase-js": "^2.106.0",
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "ws": "^8.20.1"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10"
  }
}